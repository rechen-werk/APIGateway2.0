@using APIGateway2._0.Models
<div class="hacky-align">
	<h1>Shops</h1>
	<ul class="tilesWrap">
        @foreach (var shop in (IList<ShopAgent>)ViewData["shops"])
        {
    	    <li>
    		    <h3>@shop.Name</h3>
    		    <p>@shop.Description</p>
    		    <button onclick="location.href='@Url.Action("ShopVisualizer", "Home", shop, null)'">View »</button>
    	    </li>
        }
    </ul>
</div>
